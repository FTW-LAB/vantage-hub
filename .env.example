# Public hostname (no scheme)
VITE_PUBLIC_HOSTNAME=ftwlab.com

# GitHub identity for flywheel (public org + human)
VITE_PUBLIC_GITHUB_ORG=ftwforge
VITE_PUBLIC_GITHUB_HUMAN=wantzjt

# Optional server-only: read-only PAT for higher public API rate limits
# NEVER commit real tokens. Set in Vercel Project → Settings → Environment Variables.
# GITHUB_TOKEN=
